# stockMarket_LSTM_prediction
The goal of this project is to segment the market into distinct patterns using a detection algorithm and provide the LSTM model with sequential data to predict future prices based on these patterns
